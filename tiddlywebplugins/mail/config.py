"""
default configuration settings
"""
config = {
    'email': {
        'subscription_format': 'subscriptions.${subscription}',
        'default_subscription': 'daily',
        'bag_mapping': '$bag' #eg: ${bag}_private
    }
}
